/*
 * Hardhat EVM network config for local development.
 * This is not used in staging or production.
 *
 * Local environment: Hardhat network
 * Staging environment: Test network (Goerli, mumbai, etc)
 * Production environment: Main network (Homestead, matic, etc)
 *
 */

module.exports = {
  defaultNetwork: 'hardhat',
};
